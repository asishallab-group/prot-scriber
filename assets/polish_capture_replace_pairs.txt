# Pairs of lines rewriting a FINISHED human readable description, applied after one has been
# chosen. See capture_replace_pairs.txt for the format; this is the same thing at the other end of
# the process.
#
# FORMAT. Lines in PAIRS: an expression, then the replacement below it. (The other seven shipped
# lists are not paired -- they are one expression per line. Only these two capture-replace
# lists take a replacement.) An expression never spans lines.
#
# A comment is always ignored, and so is a blank line -- EXCEPT the line directly after an
# expression, which is that expression's replacement whatever it contains. A blank one there
# means 'delete what matched', and one holding a single space means 'replace it with a space';
# the pairs below use both. So leave no blank line between an expression and its replacement,
# and put them between pairs freely. Write a literal '#' in an expression as '[#]'.

# A dangling conjunction or preposition left at the end of a description, as in 'arm repeat protein
# interacting with'. The words themselves are non-informative and are kept where they join two
# things; at the end they join nothing.
(?i)\s*\b(and|or|the|from|to)\b\s*$

# The sentinel a domain accession was joined with, which has done its work by now: it held
# 'duf~4228' together as one word through the rules that would have taken its number away and
# through the splitting, and a reader wants to see 'duf4228'.
~


